const chai = require('chai');

var expect = chai.expect,
    intialize;

describe ('Main executable', () => {

  describe ('when valid token is passed in', () => {
    it ('should call init function for bot', () => {
    });
  });

  describe('when invalid token is passed in', () => {
    it ('should throw an error', () => {
    });
  });
});